import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import Icon from "./Icon";

<Meta
  title="Icon"
  component={Icon}
  argTypes={{
    name: {
      type: "string",
      control: {
        type: "select",
        options: ["alert", "search"],
      },
    },
  }}
/>

# Icon

export const Template = (args) => <Icon {...args} />;

<Canvas>
  <Story
    name="Default"
    args={{
      name: "alert",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
