import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import {
  Card,
  CardBadge,
  CardContent,
  CardMedia,
  CardMeta,
  CardTitle,
} from "@whitespace/components";
import styled from "@emotion/styled";

export const Badge = styled.span`
  display: grid;
  align-items: center;
  text-align: center;
  justify-items: center;
  font-size: 2rem;
  background: var(--color-blue-500);
  color: #fff;
  font-weight: 600;
  padding: 0.25rem;
  width: 3rem;
  height: 3rem;
  border-radius: 100%;
  text-transform: uppercase;
`;

<Meta title="Card" parameters={{ actions: { argTypesRegex: null } }} />

<Canvas>
  <Story name="Default" args={{}}>
    {(args) => (
      <Card {...args}>
        <CardContent>
          <CardTitle>Title</CardTitle>
          <CardMeta>2021-12-12</CardMeta>
          <p>Lorem ipsum</p>
        </CardContent>
        <CardMedia
          image={{
            src: "https://picsum.photos/seed/picsum/600/300",
          }}
        />
        <CardBadge>
          <Badge>★</Badge>
        </CardBadge>
      </Card>
    )}
  </Story>
  <Story
    name="Link"
    args={{
      link: { url: "/home" },
    }}
  >
    {(args) => (
      <Card {...args}>
        <CardContent>
          <CardTitle>Title</CardTitle>
          <CardMeta>2021-12-12</CardMeta>
          <p>Lorem ipsum</p>
        </CardContent>
        <CardMedia
          image={{
            src: "https://picsum.photos/seed/picsum/600/300",
          }}
        />
      </Card>
    )}
  </Story>
  <Story
    name="Button"
    args={{
      link: { onClick: () => {} },
    }}
  >
    {(args) => (
      <Card {...args}>
        <CardContent>
          <CardTitle>Title</CardTitle>
          <CardMeta>2021-12-12</CardMeta>
          <p>Lorem ipsum</p>
        </CardContent>
        <CardMedia
          image={{
            src: "https://picsum.photos/seed/picsum/600/300",
          }}
        />
      </Card>
    )}
  </Story>
</Canvas>
