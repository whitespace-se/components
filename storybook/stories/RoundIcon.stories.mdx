import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { RoundIcon, IconProvider } from "@whitespace/components";
import alertIcon from "../static/icons/alert-circle.svg";
import searchIcon from "../static/icons/search.svg";

<Meta
  title="RoundIcon"
  component={RoundIcon}
  argTypes={{
    name: {
      type: "string",
      control: {
        type: "select",
        options: ["alert-circle", "search"],
      },
    },
    backgroundColor: {
      type: "string",
      control: {
        type: "color",
      },
    },
    color: {
      type: "string",
      control: {
        type: "color",
      },
    },
    src: {
      type: "string",
      control: {
        type: "select",
        options: [alertIcon, searchIcon],
      },
    },
  }}
/>

# Icon

<Canvas>
  <Story
    name="With src"
    args={{
      src: alertIcon,
      backgroundColor: "#d9d9d9",
      color: "#000000",
    }}
  >
    {(args) => <RoundIcon {...args} />}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="With name"
    args={{
      name: "alert-circle",
      backgroundColor: "#d9d9d9",
      color: "#000000",
    }}
  >
    {(args) => (
      <IconProvider getIconSrc={(name) => `/icons/${name}.svg`}>
        <RoundIcon {...args} />
      </IconProvider>
    )}
  </Story>
</Canvas>
